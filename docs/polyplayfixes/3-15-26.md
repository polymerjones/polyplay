
## Visual

Demo content needs to be changed. Song and artwork. Official demo content for public release. I will do this later, in a creative mode.

App Title Card Title (right before release):
Polyplay v1.0.0 



## DESKTOP BUGS

- Pressing Enter or Return on the create new polyplaylist box should trigger the Create button - not sure what this is called? UI accept?

- I'm not crazy about the Hey new person! welcome box font, it's like too bold
	- ideally this font would match my logo font which seems to be ___
 

- Rows issue appears to be resolved			
> Resolved.
  The actual fix was styles.css:5245: adding align-content: start; to .track-grid.track-grid--list. That stopped the desktop list grid from stretching its rows vertically after the welcome card was dismissed.
  The earlier spacing tweaks were mostly noise around the real issue. This one addressed the actual packing behavior.
  



- Desktop toasts should be the same color and format as iOS app has
## IOS APP BUGS


## BOTH DESKTOP AND IOS

- When track reaches the end and (repeat is off) playbar is not going to the next song on the playlist --note-- when shuffle is active, playbar plays another track when active track finishes, shuffle works as intended. the default action should be play next track when a track completes (except in Repeat mode obviously)
-
- I need to remove all worthless old buttons on the settings page. A cleanup for public release, for example, the Vault page has an inspector with debugging information I'm not sure that needs to be there for public release
	- Example: Reload Library View button, I'm not sure what this does or if it's necessary
- Also app messaging must be consistent
	- Example: Tap tiles to play • Tap artwork to build aura appears on the main menu bar -- this should say Tap artwork on the playbar to enter fullscreen mode
