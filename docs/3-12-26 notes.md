I have several fixes and features to implement:

# DESKTOP ROWS BUG
- Tested on Brave browser and Safari, track rows leading space is too large

# FULLSCREEN MODE (IOS)
- Clicking the Aura button does not make the waveform, the seek bar and the animated artwork flash the aura color

# IOS AND DESKTOP
- When user goes to settings, I think the default track that appears on the Update artwork should be the current track that's playing, so user can quickly identify the track that they want to replace the artwork for. This should be consistent for Replace Audio dropdown and Remove track dropdown (whatever track is playing is the default selected on the dropdown)

# GRATITUDE JOURNAL PAGE
- When user taps pencil icon to edit an entry, when user taps or click off of the selected entry the edit text saves whatever's typed and closes the edit box, when the user taps the pencil, that icon should transform into a 3.5 floppy disk icon (save). So when the user clicks the save icon edit mode is exited and the text changes are saved

# GRATITUDE PROMPT ON DESKTOP
- If user presses enter or return key while in the gratitude prompt text box, the prompt should submit and close

# OTHER COOL VISUAL STUFF
- On paint splatter FX mode, I had the idea of the paint splatter moving towards the viewer in z space during its life cycle. That could potentially get chaotic pretty fast.

